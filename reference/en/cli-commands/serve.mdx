---
title: 'serve'
---

The `morph serve` command starts the server for the data application built with Morph.

```bash Shell
morph serve
```

<ParamField body="--preview" type="boolean">
Starts the frontend code in build mode. This is a preview command that runs the application in a state close to deployment.
</ParamField>