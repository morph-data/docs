---
title: 'serve'
---

`morph serve`コマンドは、Morphで構築したデータアプリのサーバーを立ち上げるコマンドです。

```bash Shell
morph serve
```

<ParamField body="--preview" type="boolean">
フロントエンドのコードをbuildモードで起動します。デプロイ時の状態に近いプレビュー用の起動コマンドです。
</ParamField>