---
title: 'config'
---

`morph config`コマンドは、クラウドのMorphアカウントをローカル環境に設定するためのコマンドです。

実行結果は、`~/.morph/credentials`に保存されます。

```bash Shell
morph config
```

### Arguments

<ParamField body="--profile" type="str">
`~/.morph/credentials`に登録するプロフィール名を指定することができます。
</ParamField>